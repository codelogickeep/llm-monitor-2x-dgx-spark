# Live drop-in: /etc/systemd/system/dgx-spark-monitor.service.d/zz-monitor-v2.conf
#
# The deployment id is the namespace for stored samples, stats and alerts, so it
# must change together with the served model: switching lanes starts a fresh
# baseline instead of mixing two deployments in one history.
[Service]
Environment=DGX_MONITOR_DEPLOYMENT_ID=deepseek-v4-flash-vision-exp-prod-20260921
